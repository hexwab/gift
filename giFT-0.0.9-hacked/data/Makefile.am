# there's gotta be a better way to do this...anyone?
if GNUTELLA
SUBDIRS = OpenFT Gnutella
else
SUBDIRS = OpenFT
endif

EXTRA_DIST = mime.types

giFTdatadir = $(giftdatadir)
giFTdata_DATA = mime.types
